//rfc without export line
//rfcealong with export line
import React, { useEffect, useState } from 'react'
import Header from './Header'
function SuggestedVideo(){
    return{
        <div className='suggested-video'>
        <className='suggested image'>
        <img src=''
    }
}

function Video() {
    // const[seconds, setSeconds]=useState(0)
    // const targetTime=Math.floor((new Date()).getTime()/1000 + 30);  //add 30 seconds

    // //when webpage renders
    // useEffect(() => {
    //     const interval=setInterval(()=> {
    //         const currentTime=Math.floor((new Date()).getTime()/1000);

    //         const remainingTime=targetTime-currentTime;
            
    //         if(remainingTime>=0){
    //             setSeconds(remainingTime);
    //         }
    //     })
    // },1000)
    //useEffect(function(){
    //     useEffect(()=>{
    //         const currentTime=(new Date()).getTime()/1000;
    //         const remainingTime= targetTime-currentTime;
    //         setSeconds(Math.floor(remainingTime));
    //         if(remainingTime >=0){
    //     const interval=setInterval()
    // }
  return (
    <div>
        <p>Video Component</p>
        {/* <h2>Time left:{seconds}</h2> */}
      
    </div>
  )
}

export default Video;
